Go Simple API успешно установлен!

Проверьте статус подов:
  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }}"

{{- if .Values.ingress.enabled }}

Ingress настроен для хоста: {{ .Values.ingress.host }}

Добавьте запись в /etc/hosts:
  127.0.0.1 {{ .Values.ingress.host }}

Проверьте доступность:
  curl http://{{ .Values.ingress.host }}/health
{{- else }}

Для доступа к API используйте port-forward:
  kubectl port-forward svc/{{ include "go-simple-api.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}

Проверьте доступность:
  curl http://localhost:8080/health
{{- end }}
