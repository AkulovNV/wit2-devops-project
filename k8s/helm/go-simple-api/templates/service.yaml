apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-simple-api.fullname" . }}
  labels:
    {{- include "go-simple-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "go-simple-api.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
